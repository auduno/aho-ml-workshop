function setup() {
  createCanvas(400, 400);
  background("yellow");
  
  // set size of any text we draw
  textSize(32);
  // draw some text on canvas
  text("hello!", 100, 200);
  
  // change color of anything we draw
  fill("red");
  // change text size of anything we draw
  textSize(40);
  // draw some more text on canvas
  text("hallo!", 100, 300);
}

function draw() {
}